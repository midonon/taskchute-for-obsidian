# Task Plan: 見積時間UIの操作性・レイアウト改善

## Goal

見積時間を新規タスクの基本入力へ移し、タスク行から直接編集できるようにし、見積・実績を分かる表記で比較できるようにする。日本語表示と狭い画面でのレイアウトも改善する。

## Current Phase

Phase 5: ユーザーによるObsidian実機確認待ち

## Phases

### Phase 1: 要件・現状調査

- [x] 添付スクリーンショットと要求を確認する
- [x] 作成画面、見積チップ、行レイアウト、i18n、関連テストを調査する
- [x] 原因を記録する
- **Status:** complete

### Phase 2: 実装計画の確認

- [x] 最小の修正範囲とPC／モバイルの表示方針を決める
- [x] テスト・検証の範囲を決める
- [x] ユーザーの計画承認を受ける
- **Status:** complete

### Phase 3: 小さな実装（承認後）

- [x] タスク名直下に常時表示する見積時間入力を追加する
- [x] 新規作成と既存の編集モーダルで、空欄または1以上の整数だけを許可する
- [x] 見積チップを直接編集ボタンにする
- [x] 完了タスクの実績時間を、見積と同じ分単位・ラベル付きで表示する
- [x] 右端の操作領域をまとめ、Gridの列数とDOMを整合させる
- [x] 日英辞書へ見積時間関連キーを追加する
- [x] 関連するユニットテストを追加・更新する
- **Status:** complete

### Phase 4: 検証（承認後）

- [x] `npm run typecheck`
- [x] 変更した機能の関連Jestテスト
- [x] `node node_modules/jest/bin/jest.js --runInBand`（既知の非機能2件を除き1352件成功）
- [x] `npm run lint`
- [x] `npm run build`
- [x] Windows上のObsidianを再読み込みできる状態にする
- **Status:** complete

### Phase 5: ユーザー実機確認

- [ ] ユーザーがPC・狭い画面で確認できる変更点を報告する
- [ ] 実機確認結果を受け取り、次の小さな修正へ進む
- **Status:** pending

## Implementation Outline

1. `TaskCreationController` の詳細設定から見積入力を切り離し、名前入力グループの直後に常時表示する。`type=number`、`inputmode=numeric`、`min=1`、`step=1` を指定し、送信時にも整数を検証する。
2. `EstimatedTimeModal` も同じ整数制約に揃える。空欄保存は既存どおりfrontmatterから削除する。
3. `TaskRowController` の見積表示を意味論的なbuttonにし、既存のモバイル向けtap処理で編集モーダルを開く。完了タスクの実績は、既存の経過時間計算値を総分へ変換して、`見積 30m` と並べて比較できる `実績 18m` 形式で描画する。
4. `TaskListRenderer` と `TaskChuteView` に編集モーダル用のcallbackを渡す。右端のルーチン設定・歯車ボタンをactionsコンテナへ入れ、CSS Gridを名前付き領域または同等の固定配置にする。
5. `ja.ts` と `en.ts` の `taskChuteView` 辞書へ見積・実績を含むすべての使用キーを追加し、フォールバック英語が日本語UIへ漏れないようにする。

## Display Policy

- PC: タスク名は余った幅を使い、長い名前は省略表示する。見積と完了済み実績は `見積 30m`／`実績 18m` と区別して一行に保つ。
- 640px未満: 既存方針どおりプロジェクト表示と実績時間を優先度に従い隠す。見積チップと開始・停止・右端操作は残す。
- 520px未満／モバイル: タスク名を残り幅へ縮め、右端操作コンテナは折り返さない。見積チップはbuttonとしてフォーカス・タップ時の反応を表示する。新規作成の見積入力は数字キーボードを優先する。

## Constraints

- 現在の作業ブランチだけで実施し、mainへのマージ、commit、push、GitHub上のPR・Issue・コメントは行わない。
- 既存の未コミット差分 `package-lock.json` は触れない。
- 固定ハッシュの確認・照合など、目的に直接寄与しない過剰な検証はしない。
- 作者固有のMac環境・E2EはWindowsで実行しない。ユーザーの実機確認を最終確認に使う。
- 作業報告、計画、コードコメント、コミットメッセージはリポジトリルートからの相対パスだけで記載し、ローカル絶対パスは含めない。

## Key Decisions

| Decision | Rationale |
|----------|-----------|
| 見積時間は詳細設定に依存しない基本入力にする | ユーザーが常時表示を要求しているため |
| 見積チップはbutton化する | クリック／タップ可能であることを明確にし、キーボード操作も可能にするため |
| 完了タスクの実績は総分とラベルで表示する | `00:18` の数字だけでは意味が分かりにくく、見積との比較もしづらいため |
| 右端に既存のルーチン設定と歯車をまとめる | 行内のGridセル数を固定し、歯車だけの折り返しを防ぐため |
| 新しい直接複製ボタンは追加しない | 現在の `🔄` はルーチン設定、複製は歯車メニュー内であり、要求外の挙動変更を避けるため |
| LunaMaxが応答しない場合はTerraが引き継ぐ | 実装を停止させず、共有作業ツリー上の途中変更を保全して完了させるため |

## Errors Encountered

| Error | Attempt | Resolution |
|-------|---------|------------|
| `memory/corrections/lessons.md` が存在しない | 1 | リポジトリ内に存在しないことを記録し、作業はローカル調査を継続 |
| LunaMaxが進捗・ブロッカーを返さない | 1 | 共有作業ツリーの途中テスト変更を保全し、Terraが実装・検証を引き継ぐ |
| 標準のJest／esbuild子プロセス起動がWindows制限で失敗 | 1 | テストは単一プロセスで実行し、ローカルビルドは許可済みの実行環境で完了 |
